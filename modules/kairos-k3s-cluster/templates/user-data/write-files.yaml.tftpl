write_files:
%{for file in files~}
  - path: ${file.dest}
    permissions: ${file.permissions}
    content: |
      ${indent(6, file.content)}
%{endfor~}

runcmd:
  - - systemctl
    - enable
    - --now
    - qemu-guest-agent.service
